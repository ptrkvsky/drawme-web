import styled from "@emotion/styled";

export const SVG = styled.svg`
  position: fixed;
  z-index: 100;
  left: 50%;
  top: 0;
  transform: translate(-50%);
  max-width: 40vw;
  .st1 {
    fill: transparent;
  }
`;
