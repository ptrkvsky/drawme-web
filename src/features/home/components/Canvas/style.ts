import styled from "@emotion/styled";

export const Canva = styled.div`
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  transition: opacity 0.5s ease-in-out;
  pointer-events: none;
`;
